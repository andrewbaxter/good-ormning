[package]
name = "integration_tests"
version = "0.3.14"
edition = "2021"
publish = false

[dependencies]
chrono = ">=0"
jiff = ">=0"
rusqlite = ">=0"
tokio = { version = ">=0", features = ["macros", "rt"] }
"good-ormning" = { path = "..", features = [
    "chrono",
    "pg",
    "sqlite",
    "jiff",
    "import",
    "deadpool",
] }
"good-ormning-macros" = { path = "../macros" }
tokio-postgres = { version = ">=0", features = [
    "with-chrono-0_4",
    "with-jiff-0_2",
] }
loga = ">=0"
good-ormning-core = { version = "0.5.14", path = "../core" }
flowcontrol = ">=0"
tempfile = "3.27.0"
pglite-oxide = { git = "https://github.com/f0rr0/pglite-oxide" }
deadpool-postgres = { version = "0.12", features = ["rt_tokio_1"] }
deadpool-sqlite = { version = "0.12", features = ["rt_tokio_1"] }

[build-dependencies]
"good-ormning" = { path = "..", features = ["chrono", "jiff", "pg", "sqlite"] }
flowcontrol = ">=0"

[lints.clippy]
all = "allow"
